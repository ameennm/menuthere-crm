{
    "name": "menuthere-crm-api",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "dev": "wrangler dev",
        "deploy": "wrangler deploy",
        "db:init": "wrangler d1 execute menuthere_db --remote --file=./schema.sql"
    },
    "devDependencies": {
        "wrangler": "^4.0.0"
    }
}